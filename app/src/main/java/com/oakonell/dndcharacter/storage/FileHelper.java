package com.oakonell.dndcharacter.storage;

import android.content.Context;

import com.oakonell.dndcharacter.model.*;
import com.oakonell.dndcharacter.model.Character;

import org.w3c.dom.Document;

import java.io.FileOutputStream;

/**
 * Created by Rob on 10/26/2015.
 */
public class FileHelper {
//    public static CharacterInfo readCharacters() {
//
//    }

    public static Character readCharacter() {
    return null;
    }

    public static void saveCharacter(Context context, Character character) {
/*        String fileName = character.getName() + " .xml";
        try {
            FileOutputStream outputStream = context.openFileOutput(fileName, Context.MODE_PRIVATE);
            Document doc = character.toXML();

            outputStream.write(string.getBytes());
            outputStream.close();

        } catch (Exception e) {

        }
        */
    }
}
